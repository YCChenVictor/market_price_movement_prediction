# run test:
# docker build -f Dockerfile.test -t market-price-movement-prediction-test .
# docker run --rm market-price-movement-prediction-test

FROM python:3.9-slim

WORKDIR /app

COPY requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["pytest"]
